{{>header}}
    <div class="container mt-4">
        <div class="row">
            <div class="col-sm6 detail-image">
               <img src="{{blog.image}}" alt="{{blog.title}}"> 
           </div>
            <div class="col-sm-6">
                <h1>{{blog.title}}</h1>
               
                <p>{{blog.description}}</p>
        </div>
    </div>
      </div>
{{>footer}}